const screens=[...document.querySelectorAll('.screen')];function show(id){screens.forEach(s=>s.classList.toggle('active',s.id===id));window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go));
function choices(correct,min=0,max=9){let s=new Set([correct]);while(s.size<4)s.add(Math.floor(Math.random()*(max-min+1))+min);return [...s].sort(()=>Math.random()-.5)}function render(el,vals,fn){el.innerHTML='';vals.forEach(v=>{let b=document.createElement('button');b.className='choice';b.textContent=v;b.onclick=()=>fn(v,b);el.appendChild(b)})}function lock(el){el.querySelectorAll('button').forEach(b=>b.disabled=true)}
const lf=[3,7,5,9];let li=0,ls=1,lfirst=0;const lEq=learnEq,lPr=learnPrompt,lBu=learnBuild,lCh=learnChoices,lFe=learnFeedback,lNe=learnNext;function startLearn(){li=0;loadLearn();show('learn')}function loadLearn(){let f=lf[li];ls=1;lfirst=f-1;lEq.textContent=`9 × ${f}`;lPr.textContent=`What's one less than ${f}?`;lBu.textContent='_ _';lFe.className='feedback';lFe.textContent='Choose the answer that makes sense.';lNe.classList.add('hidden');learnPL.textContent=`Example ${li+1} of ${lf.length}`;learnPF.style.width=`${li/lf.length*100}%`;render(lCh,choices(lfirst),learnPick)}function learnPick(v,b){let f=lf[li];if(ls===1){if(v===lfirst){b.classList.add('correct');lock(lCh);lBu.textContent=`${lfirst} _`;lFe.className='feedback success';lFe.textContent=`Exactly. One less than ${f} is ${lfirst}.`;setTimeout(()=>{ls=2;let s=9-lfirst;lPr.textContent=`What goes with ${lfirst} to make 9?`;render(lCh,choices(s),learnPick)},600)}else{b.classList.add('incorrect');lFe.className='feedback try';lFe.textContent=`Not quite. Count back one from ${f}.`}}else{let s=9-lfirst;if(v===s){b.classList.add('correct');lock(lCh);lBu.textContent=f*9;lPr.textContent=`9 × ${f} = ${f*9}`;lFe.className='feedback success';lFe.innerHTML=`Excellent thinking!<br>One less than ${f} is ${lfirst}. ${lfirst} and ${s} make 9.`;learnPF.style.width=`${(li+1)/lf.length*100}%`;lNe.textContent=li===lf.length-1?'Finish lesson':'Next example';lNe.classList.remove('hidden')}else{b.classList.add('incorrect');lFe.className='feedback try';lFe.textContent=`Try again. What number added to ${lfirst} makes 9?`}}}lNe.onclick=()=>{if(li<lf.length-1){li++;loadLearn()}else show('learnComplete')};document.getElementById('startLearn').onclick=startLearn;document.getElementById('learnRestart').onclick=startLearn;
const gf=[6,4,8,2,7];let gi=0,gs=1,gfirst=0,gcorrect=0;function startGuided(){gi=0;gcorrect=0;loadGuided();show('guided')}function loadGuided(){let f=gf[gi];gs=1;gfirst=f-1;guidedEq.textContent=`9 × ${f}`;guidedPrompt.textContent=`What's one less than ${f}?`;guidedBuild.textContent='_ _';guidedFeedback.className='feedback';guidedFeedback.textContent='Use the Secret one step at a time.';guidedNext.classList.add('hidden');guidedScore.textContent=`${gi} / ${gf.length}`;render(guidedChoices,choices(gfirst),guidedPick)}function guidedPick(v,b){let f=gf[gi];if(gs===1){if(v===gfirst){b.classList.add('correct');lock(guidedChoices);guidedBuild.textContent=`${gfirst} _`;guidedFeedback.className='feedback success';guidedFeedback.textContent=`Yes. One less than ${f} is ${gfirst}.`;setTimeout(()=>{gs=2;guidedPrompt.textContent=`What goes with ${gfirst} to make 9?`;render(guidedChoices,choices(9-gfirst),guidedPick)},550)}else{b.classList.add('incorrect');guidedFeedback.className='feedback try';guidedFeedback.textContent=`Count back one from ${f}.`}}else{let s=9-gfirst;if(v===s){b.classList.add('correct');lock(guidedChoices);gcorrect++;guidedBuild.textContent=f*9;guidedPrompt.textContent=`9 × ${f} = ${f*9}`;guidedFeedback.className='feedback success';guidedFeedback.textContent='Great strategy! You built the answer.';guidedScore.textContent=`${gi+1} / ${gf.length}`;guidedNext.textContent=gi===gf.length-1?'See result':'Next card';guidedNext.classList.remove('hidden')}else{b.classList.add('incorrect');guidedFeedback.className='feedback try';guidedFeedback.textContent=`What number goes with ${gfirst} to make 9?`}}}guidedNext.onclick=()=>{if(gi<gf.length-1){gi++;loadGuided()}else{pcTitle.textContent='Excellent thinking!';pcText.textContent=`You built ${gcorrect} answers using the Secret of 9.`;show('practiceComplete')}};guidedStart.onclick=startGuided;guidedRestart.onclick=startGuided;
const qf=[2,8,4,7,3,6,9,5,1,10];let qi=0,qcorrect=0,qanswered=false;function startQuick(){qi=0;qcorrect=0;loadQuick();show('quick');setTimeout(()=>quickInput.focus(),100)}function loadQuick(){qanswered=false;let f=qf[qi];quickEq.textContent=`9 × ${f}`;quickInput.value='';quickInput.disabled=false;quickCheck.disabled=false;quickFeedback.className='feedback';quickFeedback.textContent='Type your answer, then check it.';quickNext.classList.add('hidden');quickScore.textContent=`Card ${qi+1} of ${qf.length}`;setTimeout(()=>quickInput.focus(),100)}function checkQuick(){if(qanswered)return;let f=qf[qi],a=Number(quickInput.value);if(quickInput.value.trim()===''){quickFeedback.className='feedback try';quickFeedback.textContent='Type an answer first.';return}qanswered=true;quickInput.disabled=true;quickCheck.disabled=true;if(a===f*9){qcorrect++;quickFeedback.className='feedback success';quickFeedback.textContent='Exactly! You used the Secret perfectly.'}else{let first=f-1,second=9-first;quickFeedback.className='feedback try';quickFeedback.innerHTML=`The answer is ${f*9}.<br>One less than ${f} is ${first}, and ${first} with ${second} makes 9.`}quickNext.textContent=qi===qf.length-1?'See result':'Next card';quickNext.classList.remove('hidden')}quickCheck.onclick=checkQuick;quickInput.onkeydown=e=>{if(e.key==='Enter')qanswered?quickNext.click():checkQuick()};quickNext.onclick=()=>{if(qi<qf.length-1){qi++;loadQuick()}else{pcTitle.textContent=qcorrect>=8?'Fantastic strategy!':'Strong effort!';pcText.textContent=`You answered ${qcorrect} out of ${qf.length} correctly.`;show('practiceComplete')}};quickStart.onclick=startQuick;quickRestart.onclick=startQuick;
function startShop(){shopInput.value='';shopInput.disabled=false;shopCheck.disabled=false;shopFeedback.className='feedback';shopFeedback.textContent='One less than 7 is 6. What makes 9?';show('shop');setTimeout(()=>shopInput.focus(),100)}function checkShop(){if(shopInput.value.trim()===''){shopFeedback.className='feedback try';shopFeedback.textContent='Type your answer first.';return}if(Number(shopInput.value)===63){shopFeedback.className='feedback success';shopFeedback.textContent='Exactly! 9 bags of 7 apples make 63 apples.';shopInput.disabled=true;shopCheck.disabled=true;setTimeout(()=>show('shopComplete'),850)}else{shopFeedback.className='feedback try';shopFeedback.textContent='Try again. One less than 7 is 6. What goes with 6 to make 9?'}}document.getElementById('startShop').onclick=startShop;document.getElementById('shopRestart').onclick=startShop;document.getElementById('shopCheck').onclick=checkShop;shopInput.onkeydown=e=>{if(e.key==='Enter')checkShop()};
